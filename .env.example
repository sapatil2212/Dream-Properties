# Frontend Environment Variables (Vite)
# For Vercel single-bundle deployment: leave empty or set to empty string
# For separate backend deployment: set to your backend URL
# For local development: use .env.local with http://localhost:5000
VITE_API_URL=

# Gemini API Key (if using AI features)
GEMINI_API_KEY=your-gemini-api-key

# Backend Environment Variables (Express)
# Copy these to your backend hosting platform (Render/Railway)

# Database Configuration
DB_HOST=your-database-host
DB_PORT=4000
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_NAME=your-database-name
DB_SSL_CA=-----BEGIN CERTIFICATE-----\nyour-ssl-certificate\n-----END CERTIFICATE-----

# JWT & Session Secrets
JWT_SECRET=your-jwt-secret-key
SESSION_SECRET=your-session-secret-key

# Email Configuration (Nodemailer)
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Super Admin Credentials
SUPER_ADMIN_EMAIL=admin@example.com
SUPER_ADMIN_PASSWORD=your-admin-password

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
